rules_version = '2';

// All client access is funnelled through Cloud Functions (Admin SDK bypasses
// these rules). Direct Firestore reads/writes from clients are blocked.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
