extends Area2D

# ── Собака капитана ──────────────────────────────────────────────────────────
# Часть боя с [[Босс — Капитан полиции]]. Капитан спускает её с поводка, она
# ЛЕТИТ В НОРМАЛЬДО — и дальше одно из двух:
#
#   а) промахнулась и ушла в стену — ОТСКАКИВАЕТ и идёт снова, уже под другим
#      углом. Так до восьми раз;
#   б) восьмой отскок она ВСЕГДА направляет к хозяину и возвращается к нему.
#
# ── ЗАЧЕМ ОТСКОКИ ──────────────────────────────────────────────────────────
# Атака, которая летит в тебя один раз, читается по прямой: сошёл с линии —
# всё. Отскакивающая заставляет читать ГЕОМЕТРИЮ КОМНАТЫ: она вернётся, и
# откуда — зависит от того, в какую стену её отправил твой уход. Это тот же
# вопрос, что у батаранга, но заданный противником, а не игроком.
#
# ── ПОЧЕМУ ВОСЬМОЙ ОТСКОК — К ХОЗЯИНУ ──────────────────────────────────────
# Атака обязана КОНЧАТЬСЯ, и кончаться понятно. Собака, скачущая по экрану, пока
# не попадёт, — это не атака, а фон, от которого нельзя отдохнуть; а исчезнувшая
# в никуда обманывает: игрок не понял, кончилось или нет. Уход к хозяину виден и
# читается как «отбегался».
#
# Восемь — это столько, сколько нужно, чтобы отскок перестал быть случайностью и
# стал правилом, и при этом не столько, чтобы надоесть: на пятом игрок уже ждёт
# её возвращения и смотрит на стены, а не на неё.
#
# ── И ДВЕ ВЕЩИ, КОТОРЫЕ ДЕЛАЮТ ЕЁ СОБАКОЙ, А НЕ СНАРЯДОМ ───────────────────
# Она РАЗГОНЯЕТСЯ всё время, пока спущена, и она ЕСТ СНАРЯДЫ — любые, от любого
# скина. Оба правила ниже расписаны там, где живут; вместе они говорят одно:
# отбиться от неё нельзя, можно только уйти, и с каждой секундой уходить труднее.

# ── ЧЕТЫРЕ КАДРА ДОБЕРМАНА ─────────────────────────────────────────────────
# Собака у капитана НА ЦЕПИ, пока он её держит, и БЕЗ ЦЕПИ, когда сорвалась:
# цепь — единственное, по чему видно, отпустил он её уже или ещё нет.
#
# И пасть. По умолчанию закрыта, а на подлёте к Нормальдо раскрывается — тем же
# языком, что и сам Нормальдо, открывающий рот на пролетающую мимо пиццу. Пасть,
# раскрытая всё время полёта, ничего не сообщает; раскрывшаяся за полметра до
# игрока сообщает «сейчас укушу».
const TEX_LEASHED := preload("res://assets/bosses/police/dog/leashed.png")
const TEX_STRAIN  := preload("res://assets/bosses/police/dog/strain.png")
const TEX_RUN     := preload("res://assets/bosses/police/dog/run.png")
const TEX_BITE    := preload("res://assets/bosses/police/dog/bite.png")
const SFX_BARK  := preload("res://assets/audio/dog.mp3")

const ItemAura := preload("res://scripts/item_aura.gd")

# Доберман ЗАМЕТНО КРУПНЕЕ головы Нормальдо (99 px): он выбегает рядом с
# капитаном, у которого голова 150, и мерялся всегда с ней. На 72 он читался
# щенком, на 104 — просто собакой; в кадре, где стоят капитан, баннер и стая
# сватовцев, «просто собака» теряется.
const DOG_PX : float = 132.0

# ── ЕЁ ВИДНО НА ТЁМНОЙ АРЕНЕ ────────────────────────────────────────────────
# Арена у капитана — тёмная кирпичная стена, и доберман на ней нарисован почти
# в цвет: чёрно-рыжий на чёрно-буром. Размер это чинит наполовину — силуэт
# становится больше, но по-прежнему сливается, и первый заход игрок читает не
# по собаке, а по тому, что в него что-то прилетело.
#
# Поэтому под ней горит аура — тем же общим кирпичом, что у кобры (item_aura),
# и с той же СЛАБОЙ яркостью: это метка «вот она», а не приглашение подобрать.
# Цвет — красный сигнальный: он же у телеграфа перчатки и у зоны молотова, то
# есть на языке игры означает «бьёт».
const AURA_COLOR : Color = Color(1.00, 0.22, 0.12)
const AURA_A     : float = 0.48
# Аура ШИРЕ собаки: свечение ровно по силуэту читается как обводка спрайта, а
# не как свет под ним.
const AURA_K     : float = 1.35
# Дыхание ауры. Медленнее, чем у кобры: собака и так дёргается по экрану, и
# частый пульс поверх её рывков превращается в мельтешение.
const AURA_HZ    : float = 3.2

# И ПЫЛЬ ИЗ-ПОД ЛАП — чтобы след читался даже на самом быстром заходе, когда
# собака за кадр проходит семнадцать пикселей и глаз видит пунктир.
const DUST_N     : int   = 14

# ── СКОЛЬКО ОНА ВИСИТ НА ЦЕПИ ──────────────────────────────────────────────
# Меньше полусекунды, и это не пауза ради красоты: за неё игрок успевает
# ПРОЧИТАТЬ, откуда сейчас полетит. До сих пор собака появлялась уже в броске, и
# первая волна читалась как «что-то прилетело справа».
#
# Внутри этой паузы она рвётся с цепи — кадр со раскрытой пастью, — и по нему
# видно, что сейчас отпустят.
const LEASH_T  : float = 0.42
const STRAIN_AT: float = 0.20

# На каком расстоянии от Нормальдо раскрывается пасть. Чуть больше её собственной
# длины: пасть должна успеть раскрыться ДО того, как она поравняется с ним, а не
# в момент касания.
const OPEN_R : float = 96.0

# ── ОНА РАЗГОНЯЕТСЯ ВСЁ ВРЕМЯ, ПОКА СПУЩЕНА ────────────────────────────────
# Собака, идущая с одной скоростью, читается через два отскока: игрок понял темп
# и дальше просто отходит в сторону по расписанию. Разгон ломает это расписание —
# каждый следующий заход быстрее предыдущего, и то, как ты уходил от четвёртого,
# от седьмого уже не спасает.
#
# И он же УКОРАЧИВАЕТ атаку, а не удлиняет: чем быстрее собака, тем быстрее она
# добирает свои восемь отскоков. Разгон тут — не «стало злее», а «стало короче и
# злее», и это ровно то, чего просит акт, который идёт дважды подряд.
const SPEED_0 : float = 430.0   # с чего срывается с поводка
const ACCEL   : float = 110.0   # и сколько добирает за каждую секунду

# ── ПОТОЛОК — ТЕХНИЧЕСКИЙ, А НЕ ВКУСОВОЙ ───────────────────────────────────
# Перекрытие областей проверяется в ДИСКРЕТНЫХ положениях, раз в кадр. Собака,
# шагающая за кадр дальше, чем сумма полуразмеров её и Нормальдо, ПРОЛЕТАЕТ
# СКВОЗЬ него, ни разу не оказавшись с ним в одной точке, — и удар, который игрок
# видел глазами, не случается.
#
# Хитбокс собаки 87×77 (доли от DOG_PX, см. `_ready`), у Нормальдо того же
# порядка; на 60 кадрах 1050 px/c — это 17.5 пикселя за кадр, с большим запасом.
# Дальше начинается не «сложно», а «нечестно».
const SPEED_MAX : float = 1050.0

# Сколько раз она может отбиться от стены. Восьмой — всегда к хозяину.
const MAX_BOUNCES : int = 8
# Поля, за которые она не заезжает: стены арены.
const MARGIN : float = 26.0

# ── ОНА ПЕРЕХВАТЫВАЕТ СНАРЯДЫ ЛЮБОГО СКИНА ─────────────────────────────────
# Батаранг, паутина, карты, шар мага, рыгалити — всё, что вылетает из рук
# Нормальдо, собака ЛОВИТ ЗУБАМИ и гасит. Она единственная в игре, кто это
# делает, и потому от неё нельзя отбиться спеллом: только уходить.
#
# Ловит она их САМА, покадровым замером расстояния, а не физикой. У снаряда
# collision_layer = 0 — он никому не виден, он видит всех; группа `skill_shot` и
# есть единственный способ его найти (см. skill_projectile.gd).
const SHOT_GROUP : String = "skill_shot"
# Пасть шире хитбокса: снаряд, погасший ровно в точке касания, читался бы как
# «прошёл насквозь и исчез сам».
const BITE_R : float = 54.0

var damage : int = 1

# Кого ловит и к кому возвращается — ставит босс.
var target : Node2D = null
var owner_node : Node2D = null

# ── ОХОТИТСЯ ИЛИ ПРОСТО НОСИТСЯ ────────────────────────────────────────────
# Первая собака идёт ЗА НОРМАЛЬДО и после каждого отскока снова берёт на него
# курс. Она одна, и вопрос от неё — «прочитай, откуда она вернётся».
#
# Стая (вторая волна — две, третья — три) охотиться НЕ ДОЛЖНА. Три собаки,
# каждая из которых доворачивает на игрока, — это не три вопроса, а один и тот
# же, заданный втройне: куда ни уйди, все три уже летят туда. Носящиеся по
# случайным углам собаки заполняют арену, а не преследуют, и уходить от них надо
# из МЕСТА, а не с линии.
var hunts : bool = true

signal came_home

var _vel      : Vector2 = Vector2.ZERO
var _speed    : float   = SPEED_0
var _bounces  : int     = 0
var _homing   : bool    = false   # последний заход — к хозяину
var _sprite   : Sprite2D = null
var _glow     : Sprite2D = null
var _dust     : CPUParticles2D = null
var _glow_t   : float   = 0.0
var _done     : bool    = false
var _leashed  : bool    = true    # пока держат — с места не двигается

func _ready() -> void:
	collision_layer = 2
	collision_mask  = 0
	add_to_group("obstacle")
	add_to_group("police_dog")
	# ВЫШЕ ХОЗЯИНА. Капитан стоит на z=40, а собака выходит у его ног — на общем
	# слое её просто не было видно, она целиком пряталась за его головой, и вся
	# пауза на цепи проходила за кадром. Пролетая мимо него потом, она тоже
	# обязана идти спереди: собака, ныряющая ему в затылок, читается как ошибка
	# рисования.
	z_index = 41

	# Аура ПЕРВОЙ: она ложится под собаку, а порядок детей задаёт порядок
	# отрисовки внутри одного z_index.
	_glow = ItemAura.make(AURA_COLOR, DOG_PX * AURA_K, AURA_A)
	add_child(_glow)

	_dust = _make_dust()
	add_child(_dust)

	_sprite = Sprite2D.new()
	_sprite.texture        = TEX_RUN
	_sprite.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST
	_sprite.z_index        = 2
	# МАСШТАБ И ОПОРА СЧИТАЮТСЯ ОДИН РАЗ, по бегущему кадру, и дальше не трогаются.
	# Все четыре кадра нарисованы в одном поле 600×600 и в одном масштабе, так что
	# их взаимная раскладка уже задана художником; пересчёт под каждый кадр
	# отдельно ломал бы её — собака меняла бы размер и подпрыгивала ровно в тот
	# момент, когда раскрывает пасть.
	ItemSizing.fit_sprite_content(_sprite, DOG_PX)
	ItemSizing.anchor_sprite(_sprite, 0.5, 0.5)
	_sprite.texture        = TEX_LEASHED
	add_child(_sprite)

	var cs   := CollisionShape2D.new()
	var rect := RectangleShape2D.new()
	rect.size = Vector2(DOG_PX * 0.66, DOG_PX * 0.58)
	cs.shape  = rect
	add_child(cs)

	_hold_leash()

# ── СНАЧАЛА НА ЦЕПИ, ПОТОМ БЕЗ ЦЕПИ ────────────────────────────────────────
# Капитан её ДЕРЖИТ, и это видно: кадр с цепью, рывок с раскрытой пастью — и
# только потом бросок, уже без цепи. Пауза короткая, её задача одна — дать
# игроку прочитать, откуда сейчас полетит.
func _hold_leash() -> void:
	_bark()
	await _wait(STRAIN_AT)
	if not _running():
		return
	_set_frame(TEX_STRAIN)
	await _wait(LEASH_T - STRAIN_AT)
	if not _running():
		return
	# СОРВАЛАСЬ. Цепь остаётся у хозяина — с этого кадра её на собаке нет.
	_leashed = false
	_set_frame(TEX_RUN)
	_aim_at_target()
	if is_instance_valid(_dust):
		_dust.emitting = true
	_bark()

# Жива ли и в дереве — между двумя `await` её могли убрать вместе со сценой.
func _running() -> bool:
	return is_instance_valid(self) and is_inside_tree() and not _done

func _wait(sec: float) -> void:
	var t0 := Time.get_ticks_msec()
	while _running() and Time.get_ticks_msec() - t0 < int(sec * 1000.0):
		await get_tree().process_frame

# Смена кадра и ничего больше: масштаб с опорой поставлены в `_ready` по одному
# общему кадру и пересчёту не подлежат.
func _set_frame(tex: Texture2D) -> void:
	if is_instance_valid(_sprite) and _sprite.texture != tex:
		_sprite.texture = tex

func _aim_at_target() -> void:
	if not hunts:
		# Влево-и-куда-попало: угол случайный, но в левую полусферу. Совсем
		# случайный угол отправил бы половину стаи обратно за правый край, и
		# половина броска пропала бы, не долетев до поля.
		var a := randf_range(-1.15, 1.15)
		_vel = Vector2.LEFT.rotated(a) * _speed
		return
	var to : Vector2 = target.global_position if is_instance_valid(target) \
		else position + Vector2(-400.0, 0.0)
	var d := to - position
	_vel = (d.normalized() if d.length() > 1.0 else Vector2.LEFT) * _speed

func _process(delta: float) -> void:
	if _done:
		return
	# ── ПОДСВЕТКА ДЫШИТ И НА ЦЕПИ ТОЖЕ ──────────────────────────────────────
	# Это ДО выхода по `_leashed`, а не после: пауза на поводке для того и
	# сделана, чтобы игрок успел прочитать, откуда сейчас полетит, — и метка
	# нужна раньше всего именно в неё. Погаснув на эти полсекунды, она бы
	# зажигалась ровно тогда, когда читать уже поздно.
	# ── ДЫШИТ НЕГЛУБОКО ─────────────────────────────────────────────────────
	# Полный размах (0…1) гасит ауру в нижней точке почти до нуля: на снятом
	# кадре одна собака светилась, а вторая, попавшая в провал пульса, была
	# неотличима от той, что была до правки. Метку, которая через раз
	# выключается, ставить незачем — поэтому размах ПОЛОВИННЫЙ (0.5…1).
	_glow_t += delta * AURA_HZ
	ItemAura.pulse(_glow, 0.75 + 0.25 * sin(_glow_t), DOG_PX * AURA_K)
	if _leashed:
		return
	# Разгон: курс держим, скорость добираем. Направление живёт в `_vel`, поэтому
	# после каждой прибавки его надо ПЕРЕСОБРАТЬ по той же нормали — иначе
	# ускорение зависело бы от того, под каким углом она бежит.
	_speed = minf(_speed + ACCEL * delta, SPEED_MAX)
	if _vel.length() > 1.0:
		_vel = _vel.normalized() * _speed
	position += _vel * delta
	# ── СОБАКА НЕ КРУТИТСЯ ───────────────────────────────────────────────────
	# Здесь она вращалась вокруг себя, и это была ошибка жанра: кувыркается
	# БРОШЕННЫЙ предмет — бумеранг, граната, — а живое, летящее на тебя, держит
	# голову ровно. Крутящаяся собака читалась как чучело, которым кинули.
	if is_instance_valid(_sprite):
		# Морда смотрит туда, куда летит: собака, летящая затылком вперёд,
		# читается как брошенный предмет, а не как живое.
		_sprite.flip_h = _vel.x > 0.0
	# Пыль летит НАЗАД — то есть против курса. Направление берётся из `_vel`
	# каждый кадр, а не задаётся один раз: собака меняет курс на каждом отскоке,
	# и застывший шлейф показывал бы её прошлый заход.
	if is_instance_valid(_dust) and _vel.length() > 1.0:
		_dust.direction = -_vel.normalized()
	# ── ПАСТЬ РАСКРЫВАЕТСЯ НА ПОДЛЁТЕ ────────────────────────────────────────
	# Тем же языком, что и у самого Нормальдо: рот открывается на то, что летит
	# мимо. Пасть, раскрытая весь полёт, не сообщает ничего — сообщает именно
	# момент, когда она раскрылась.
	_set_frame(TEX_BITE if _near_target() else TEX_RUN)

	_eat_shots()

	if _homing:
		_check_home()
		return
	_bounce_off_walls()

func _near_target() -> bool:
	if not is_instance_valid(target):
		return false
	return global_position.distance_to(target.global_position) <= OPEN_R

# ── ЛОВИТ ЧУЖИЕ СНАРЯДЫ ────────────────────────────────────────────────────
# Всё, что вылетело из рук Нормальдо, гаснет у неё в зубах. Группа маленькая —
# на экране одновременно два-три каста, — так что покадровый обход дешевле любой
# физики, которую под это пришлось бы заводить.
func _eat_shots() -> void:
	if not is_inside_tree():
		return
	for n in get_tree().get_nodes_in_group(SHOT_GROUP):
		if not (n is Node2D) or not is_instance_valid(n):
			continue
		if global_position.distance_to((n as Node2D).global_position) > BITE_R:
			continue
		(n as Node).queue_free()
		_chomp()

# Щелчок зубами: рывок вперёд и вспышка. БЕЗ ЛАЯ — лай уже занят отскоками, и
# на очереди из трёх карт Джокера собака залаяла бы трижды подряд, перекрыв
# собственный ритм.
func _chomp() -> void:
	if not is_instance_valid(_sprite):
		return
	var tw := _sprite.create_tween()
	tw.tween_property(_sprite, "modulate", Color(2.0, 1.5, 1.4), 0.05)
	tw.tween_property(_sprite, "modulate", Color(1, 1, 1), 0.14)
	var sc := _sprite.scale
	var pop := _sprite.create_tween()
	pop.tween_property(_sprite, "scale", sc * 1.22, 0.06)\
		.set_trans(Tween.TRANS_BACK).set_ease(Tween.EASE_OUT)
	pop.tween_property(_sprite, "scale", sc, 0.12)

# ── ОТСКОК ОТ СТЕН ─────────────────────────────────────────────────────────
# Считается по КАЖДОЙ стене отдельно и с прижимом обратно в поле: иначе на
# быстрой собаке за кадр можно уехать за край настолько, что следующий кадр
# отразит её ещё раз и она залипнет в стене, дрожа на месте.
func _bounce_off_walls() -> void:
	var vp := get_viewport_rect().size
	var hit := false
	if position.x < MARGIN:
		position.x = MARGIN
		_vel.x = absf(_vel.x)
		hit = true
	elif position.x > vp.x - MARGIN:
		position.x = vp.x - MARGIN
		_vel.x = -absf(_vel.x)
		hit = true
	if position.y < MARGIN:
		position.y = MARGIN
		_vel.y = absf(_vel.y)
		hit = true
	elif position.y > vp.y - MARGIN:
		position.y = vp.y - MARGIN
		_vel.y = -absf(_vel.y)
		hit = true
	if not hit:
		return

	_bounces += 1
	_bark()
	if _bounces >= MAX_BOUNCES:
		_go_home()
		return
	# СТАЯ ОТСКАКИВАЕТ ПРОСТО ТАК: зеркальное отражение плюс небольшой разброс,
	# чтобы две собаки, вышедшие из одной точки, не залипли в общий коридор и не
	# читались как одна.
	if not hunts:
		_vel = _vel.normalized().rotated(randf_range(-0.35, 0.35)) * _speed
		return
	# А ОХОТНИЦА после отскока СНОВА берёт курс на Нормальдо — но от стены, то
	# есть с новой стороны. Чистое зеркальное отражение отправило бы её гулять по
	# треугольнику мимо игрока, и атака перестала бы быть атакой.
	if is_instance_valid(target):
		var d := target.global_position - position
		if d.length() > 1.0:
			# Половину курса берём от отражения, половину — от направления на
			# цель: так виден и отскок, и намерение.
			_vel = (_vel.normalized() + d.normalized() * 1.35).normalized() * _speed

func _go_home() -> void:
	_homing = true
	var to : Vector2 = owner_node.global_position if is_instance_valid(owner_node) \
		else Vector2(get_viewport_rect().size.x + 120.0, position.y)
	var d := to - position
	_vel = (d.normalized() if d.length() > 1.0 else Vector2.RIGHT) * _speed

func _check_home() -> void:
	var to : Vector2 = owner_node.global_position if is_instance_valid(owner_node) \
		else Vector2(get_viewport_rect().size.x + 120.0, position.y)
	# ДОВОДИМ каждый кадр: хозяин на месте не стоит, и собака, взявшая курс
	# один раз, промахнулась бы мимо него и ушла за край.
	var d := to - position
	if d.length() < 46.0 or position.x > get_viewport_rect().size.x + 100.0:
		_done = true
		came_home.emit()
		queue_free()
		return
	_vel = d.normalized() * _speed

# ── ШЛЕЙФ ИЗ-ПОД ЛАП ───────────────────────────────────────────────────────
# Искры того же красного, что и аура: два разных сигнальных цвета на одном
# существе спорили бы друг с другом, а задача у них общая — «вот она».
#
# Живут искры коротко (0.28 c) и тормозят в воздухе, поэтому след получается
# длиной в полкорпуса, а не полосой через весь экран: это подчёркивание
# движения, а не второй объект в кадре.
func _make_dust() -> CPUParticles2D:
	var p                  := CPUParticles2D.new()
	p.emitting              = false
	p.amount                = DUST_N
	p.lifetime              = 0.28
	p.spread                = 28.0
	p.gravity               = Vector2.ZERO
	p.initial_velocity_min  = 40.0
	p.initial_velocity_max  = 120.0
	p.damping_min           = 90.0
	p.damping_max           = 180.0
	p.scale_amount_min      = 2.0
	p.scale_amount_max      = 5.0
	p.color                 = Color(AURA_COLOR.r, AURA_COLOR.g, AURA_COLOR.b, 0.8)
	p.emission_shape        = CPUParticles2D.EMISSION_SHAPE_SPHERE
	p.emission_sphere_radius = DOG_PX * 0.18
	return p

func _bark() -> void:
	if not is_inside_tree():
		return
	var p := AudioStreamPlayer.new()
	p.stream    = SFX_BARK
	p.volume_db = -10.0
	add_child(p)
	p.play()
	p.finished.connect(p.queue_free)

# ── СПЕЛЛ ЕЁ НЕ БЕРЁТ ──────────────────────────────────────────────────────
# Раньше собака сбивалась спеллом и «приходила домой». Теперь она снаряды ЕСТ, и
# держать оба поведения нельзя: получилось бы, что один и тот же батаранг то
# гаснет в зубах, то убивает — в зависимости от того, кто из двух путей успел
# первым, физика или покадровый обход.
#
# Поэтому здесь — тот же ответ, что у щитоносца: щёлкнула зубами и побежала
# дальше. Молча проигнорить нельзя, иначе игрок решит, что спелл не сработал, и
# будет жать снова вместо того, чтобы уходить.
#
# Атака от этого бесконечной не становится: её по-прежнему кончают восемь
# отскоков, а с разгоном она их набирает БЫСТРЕЕ, чем набирала раньше.
func on_hit() -> void:
	if _done:
		return
	_chomp()
