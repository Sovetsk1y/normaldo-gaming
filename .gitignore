# Private signing keys — APNs .p8 auth keys, Android keystores. NEVER commit.
/secrets/
*.p8
*.keystore
*.jks

# macOS / editors
.DS_Store
.idea/
*.iml

# Obsidian per-machine state (the vault notes themselves are tracked)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/plugins/

# Claude Code per-machine settings (the shared skill in .claude/commands is tracked)
.claude/settings.local.json

# Empty leftover git init, not part of the project
dev/legacy/normaldo_gaming/ios/template/

__pycache__/
*.pyc

# Выгрузка таблицы достижений — производная от Концепция/Достижения.md,
# собирается dev/tools/achievements_xlsx.py. Закоммиченная производная
# разъезжается с исходником так же молча, как любая вторая копия данных.
Достижения — Нормальдо.xlsx
